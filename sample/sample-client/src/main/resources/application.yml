server:
  port: 8080

spring:
  application:
    name: sip-sample-client
  cloud:
    nacos:
      discovery:
        server-addr: ${BIZSIP_NACOS:127.0.0.1:8848}

bizsip:
  config-path: ${BIZSIP_CONFIG:/bizsip/config}
  integrator-url: http://sip-integrator/api/

logging:
  level:
    com.bizmda.bizsip: debug
  file:
    name: ${BIZSIP_LOG_FILENAME}

graceful:
  shutdown:
    enabled: true